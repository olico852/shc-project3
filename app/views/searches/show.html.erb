<h1>Search Results</h1>

  <% if @search.search_caregivers.empty? %>
    <p>No records found</p>
  <% else %>
    <% @search.search_caregivers.each do |c| %>
    <div class="div">
      Language: <%= c.languages %><br>
      Id: <%= c.id %><br>
      Specialties: <%= c.specialties %><br>
      Minimum years of experience: <%= c.yearsofexperience %><br>
      Gender: <%= c.gender %><br>
      <%= link_to "Book now", caregiver_path(c.id) %>
    </div>
    <% end %>
  <% end %>
